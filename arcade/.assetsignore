# Files in arcade/ that must NOT be uploaded to Cloudflare Pages.
# Only the static app shell (index.html, app.js, styles.css, _headers, data/) should ship.

# Pipeline state — contains derivative rewrites + source hashes, keep local (see ../CLAUDE.md)
.transform_cache.json

# Pipeline scripts — not part of the served site
transform_content.py
generate_content.py
__pycache__/

# Docs — repo-only, not for the public bundle
DEPLOY.md
README.md
