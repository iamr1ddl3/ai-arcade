# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node
node_modules/
dist/
build/
.next/
.turbo/

# Editors / OS
.DS_Store
.idea/
.vscode/
*.swp

# Wiki system
*.pre-wiki.bak.*
.claude/edit-log.txt

# Env
.env
.env.local
.env.*.local

# Scraped course content — never publish
tc_scrape_output/
tc_scrape_output.bak.*/

# Generated arcade content — derives from gitignored scraped content, never publish
arcade/data/

# Content transform pipeline — local only, never publish source-derived intermediates or keys
transformed/
arcade/.transform_cache.json
.transform_cache.json

# Claude Code local tooling — private workflow config, not app code
.claude/

# CI/local build output — regenerated each deploy
arcade/dist/
